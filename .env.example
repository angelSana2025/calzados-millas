# URL base del API NestJS (prefijo global, p. ej. app.setGlobalPrefix('api'))
# En desarrollo Vite hace proxy de /api → http://localhost:3000
VITE_API_URL=/api

# Producción (ejemplo):
# VITE_API_URL=https://tu-api.railway.app/api
